server:
  port: ":8080"
  corsOrigins:
    - https://repository-finder.vercel.app
    - https://miguel-armas-abt.github.io

projectType: MS

logging:
  loggingType:
    rest-server-req: false
    rest-server-res: false

errorMessages:
  Default: "No hemos podido realizar tu operación. Estamos trabajando para solucionar el inconveniente."

mongodb:
  uri: "mongodb+srv://<user>:<password>@cluster0.t2pidzg.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0&authSource=admin"
  database: githubReposDb

restClients:
  github-users:
    request:
      endpoint: https://api.github.com/users
      headers:
        provided:
          Accept: application/vnd.github.v3+json
        forwarded:
          channelId: channelId
        autoGenerated:
          timestamp: TIMESTAMP
        tracing:
          traceParent: TRACE_PARENT
      params:
        per_page: 100

multimediaStorage: https://raw.githubusercontent.com/$USER/multimedia-storage/main/repository-finder
gitHubDomain: https://github.com